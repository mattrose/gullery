form_tag({:action => 'upload'}, {:multipart => true}) do
  label do
    self << "Caption: " 
    text_field 'asset', 'caption'
  end

  label do
    self << "Select file: "
    file_field 'asset', 'file'
  end
 
  submit_tag 'Upload'
end